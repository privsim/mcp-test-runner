{
  "name": "test-runner-mcp",
  "version": "0.1.0",
  "description": "MCP server for universal test execution and result capture",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "clean": "rm -rf build",
    "build": "npm run clean && tsc && chmod +x build/index.js",
    "start": "node build/index.js",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.1.1"
  },
  "devDependencies": {
    "@swc/core": "^1.10.7",
    "@swc/jest": "^0.2.37",
    "@types/node": "^22.10.7",
    "jest": "^29.7.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.18.1"
  }
}
